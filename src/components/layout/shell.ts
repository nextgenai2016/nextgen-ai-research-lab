/**
 * Shared horizontal rhythm: aligns nav, main content, and footer on all breakpoints.
 * Scales max width and padding on very large displays so UI does not look undersized.
 */
export const layoutShell =
  "mx-auto w-full max-w-6xl px-6 sm:px-8 2xl:max-w-7xl 2xl:px-10 min-[1920px]:max-w-[min(90rem,96vw)] min-[1920px]:px-12 min-[2560px]:max-w-[min(100rem,94vw)] min-[2560px]:px-14";
